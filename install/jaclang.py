from sys import argv

curr_word = int(argv[1])
args = argv[3:]

def main():
    if curr_word == 1:
        if len(args) == 0:
        	return ["version", "versionid", "versionstr"]
        else:
    else:
        return ["aaa"]

if __name__ == "__main__":
    print(" ".join(main()))
